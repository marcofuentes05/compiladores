class Main {
    a : Int <- 5;
    b : Int;

    main() : SELF_TYPE {
        {
            a <- b + a;
            out_int(a);
            b <- 3 + 2 * (2 - 1) / 2;
            out_int(b);
        }
    };
};

